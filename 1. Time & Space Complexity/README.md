## Time Complexity
Time taken by an algorithm as a function() input size(n)

For example :- If we have a loop starting from 0 to n on an array and we are trying to print each element then the Time complexity will be O(n).

## Constant Time Complexity
Where time taken by an algorithm is constant O(1).
For example :- Finding an element inside the array on an specific position.

